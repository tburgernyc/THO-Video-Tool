// Hardcoded for local dev environment as per spec
export const API_BASE_URL = 'http://localhost:3000';
export const API_URL = `${API_BASE_URL}/api`;
export const ASSETS_URL = `${API_BASE_URL}/outputs`;