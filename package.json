{
  "name": "human-override-studio-ltx2",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\" \"npm run dev -w apps/generator\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "dev:gen": "npm run dev -w apps/generator",
    "install:all": "npm install && npm install -w apps/web && npm install -w apps/api && cd apps/generator && pip install -r requirements.txt",
    "test": "npm test -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
