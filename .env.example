# API Configuration
# NOTE: The API key variable name must be exactly API_KEY for the Gemini SDK
API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-3-pro-preview
PORT=3000
DB_PATH=./data/studio.sqlite3
GENERATOR_URL=http://localhost:8000

# Generator Configuration
HF_TOKEN=your_huggingface_token_if_needed
# Space to use for remote generation if local GPU is unavailable
VIDEO_GENERATOR_SPACE=Lightricks/ltx-2-distilled
OUTPUT_DIR=../../outputs
